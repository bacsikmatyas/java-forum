spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/forum
    username: root
    password: example
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
jwt:
  secret: asd123